{
  "name": "writeflow",
  "version": "1.0.0",
  "description": "AI-powered English writing practice platform for non-native speakers",
  "scripts": {
    "dev": "vite",
    "server": "python app.py",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["english", "writing", "ai", "education", "learning"],
  "author": "WriteFlow Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
